# BOM Party Name Mapping Report

Generated: 2026-08-14 (implementation live — re-run export script for fresh data)

Source: `production` database via `BomPartyMappingService`

> **BOM records keep their stored party names unchanged.** Mapping is for the party filter dropdown and search only.

---

## Summary (expected after API restart)

| Metric | Before | After mapping |
|--------|-------:|--------------:|
| Raw party strings in dropdown | 3,424 | — |
| Grouped filter options | — | ~900–1,100 (estimated) |
| Official `CompanyMaster` rows | 865 | 865 (unchanged) |
| BOM-only raw spellings | 2,559 | grouped into clusters |

**Regenerate this file with live counts:**

```powershell
.\scripts\export-bom-party-mapping.ps1
```

Requires API running at `http://localhost:5115`.

---

## Mapping types (implemented in code)

| Type | Code | Example |
|------|------|---------|
| **Official** | `Official` | `Baobag` |
| **Official + region** | `Official+Region` | `Greif France` ← `GREIF FLEXIBLE FRANCE SARL` |
| **BOM-only cluster** | `Cluster` | `ALKIMIA GROUP` ← `ALKIMIA`, `ALKIMIA GROUP` |
| **Singleton** | `Singleton` | one-off BOM spelling |

### Type A — Region rules (multi-country masters)

| Brand | Official regional examples |
|-------|---------------------------|
| Greif | `GREIF USA`, `Greif France`, `GREIF GERMANY`, `GREIF BENELUX`, … |
| LC Packaging | `LC Packaging France`, `L C PACKAGING UK LTD`, `LC Packaging Africa`, … |
| Cesur | `Cesur Benelux BV`, `CESUR PACKAGING (UK) LIMITED`, `Cesur USA` |
| NNZ | `NNZ Denmark`, `NNZ GERMANY`, `NNZ USA`, … |
| Storsack | `Storsack France`, `Storsack USA`, `STORSACK GERMANY`, … |
| Boxon | `Boxon Bag France`, `Boxon Bag Germany`, `Boxon Bags AB` |

### Type C — Explicit aliases

| BOM variants | Maps to |
|--------------|---------|
| `Procon`, `Procon Pacific`, `PROCON PACIFIC LIC` | `PROCON PACIFIC LLC` |
| `BOLPAMUR S.L.`, `Bolpamur SL` | `BOLPAMUR` |
| `BAOBAG`, `S.A.S. Baobag` | `Baobag` |
| `GLOBAL PAK INC`, `Global Pak, Inc` | `GLOBAL PAK` |

### Type E — BOM-only internal clusters (your examples)

| Display / group name | Member BOM spellings (unchanged in DB) |
|---------------------|----------------------------------------|
| **Alkimia** | `ALKIMIA`, `ALKIMIA GROUP` |
| **Almatis** | `Almatis`, `Almatis Alumina Private Limited`, `EXIM (ALMATIS)`, `EXIM ALMATIS` |
| **Allied Potato** | `AliiedPotato`, `ALLIEDPOTATO.COM` |
| **Allisa Ibrahim** | `Allisa ibrahim`, `Allissa ibrahim` (typo merge) |
| **Greif France** | `GREIF FLEXIBLE FRANCE`, `GREIF FLEXIBLE FRANCE SARL`, … |

Clustering uses: legal suffix stripping, first-token grouping, Levenshtein-1 merge, embedded brand detection `(ALMATIS)`, known overrides.

---

## API endpoints

| Endpoint | Purpose |
|----------|---------|
| `GET /api/bom/customers` | Grouped party list for filter dropdown (`aliasCount`, `mappingType`) |
| `GET /api/bom/party-mapping` | Full groups with all aliases (for this report) |
| `POST /api/bom/search` | `partyName` filter expands to all aliases in the group |

---

## Files

| Path | Description |
|------|-------------|
| `POApprovalAPI/Services/BomPartyMappingService.cs` | Mapping engine |
| `POApprovalAPI/Services/BomService.cs` | Uses mapping for customers + search |
| `scripts/export-bom-party-mapping.ps1` | Regenerate this report |
| `.cursor/party-research/company-master-official.csv` | Raw official names export |

---

## All mapping groups

Run `.\scripts\export-bom-party-mapping.ps1` after restarting the API to populate the full table below.

<!-- Full group table is generated by export-bom-party-mapping.ps1 -->
