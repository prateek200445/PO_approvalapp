# Copy to POApprovalAPI/.env (gitignored) when running `dotnet run` from the terminal.
# On Render: Environment → add the password keys (connection strings are in appsettings.json).

# Portal SQL — 103.240.33.122,5115
DB_PASSWORD=your-portal-sql-password

# Payroll / HR SQL — 103.240.33.122,3445
PAYROLL_DB_PASSWORD=your-payroll-sql-password

EMAIL_PASSWORD=your-smtp-password

# Ex-Im (FIBC Buyers sync) — https://www.ex-im.cloud/login
EXIM_USERNAME=info@hpbl.in
EXIM_PASSWORD=
