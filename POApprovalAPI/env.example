# Copy to POApprovalAPI/.env (gitignored) when running `dotnet run` from the terminal.
# launchSettings.json also works when starting from the IDE with a launch profile.
DB_PASSWORD=your-sql-server-password
EMAIL_PASSWORD=your-smtp-password
